import { StyleSheet } from 'react-native'

export const commonStyles = StyleSheet.create({
    container : {
        flex : 1,
        flexDirection : 'column',
        padding : 10,
    },
    title : {
        height : 30,
        fontSize : 20,
        fontFamily : 'jua',
    }
})